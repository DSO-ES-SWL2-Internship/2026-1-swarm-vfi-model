# Core deps for sender.py / receiver.py / analyze_stutter.py.
# argparse, csv, statistics, sys, time are stdlib -- no install needed.
numpy
tensorflow

# ONNX Runtime path (see machine-learning/scripts/export_onnx.py) -- lets
# receiver.py's VFI inference target GPU/NPU via onnxruntime-openvino on
# the Windows host, instead of only tf.lite.Interpreter's CPU-only path there.
onnx
onnxruntime
tf2onnx

# NOT pip-installable on Windows in any reliable way: gi (PyGObject) + the
# actual GStreamer runtime/typelibs that gi.repository.Gst needs. See
# WINDOWS_SETUP.md for the conda-forge route, which is the one that actually
# works cleanly on Windows -- plain `pip install PyGObject` on Windows either
# fails outright or installs without a working GStreamer typelib behind it.
